#include <bits/stdc++.h>
using namespace std;
int Possible(vector<int> &in,vector<int> &curr,int n,vector<int> &moves){
    if(in.size()<=curr.size()){
        if(in==curr){
            return 1;

        }
        else{
            return 0;
        }
    }
    else{
        for(int i=0;i<curr.size();i++){
            
        }
    }

}

int main() {

int t;
cin>>t;

while(t--){
    int n;
    cin>>n;
    vector<int> in(n,0);
    for(int i=0;i<n;i++){
        cin>>in[i];
    }

}
return 0;
}